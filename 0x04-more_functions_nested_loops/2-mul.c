#include <stdio.h>

/**
 * mul - check the code
 * @a: receives value
 * @b: receives assigned value
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
